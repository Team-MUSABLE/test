# --- Phase A (GPU 없음, API 무제한) ---
fastapi
uvicorn[standard]
numpy
pillow
requests
python-dotenv

# A3 depth/relief (CPU)
opencv-python-headless
# depth estimation 모델은 Phase A에서 경량 CPU 버전으로 시작 → B1에서 GPU 전환
# transformers
# torch            # B1에서 GPU 빌드로 교체

# A2 RAG
chromadb
sentence-transformers   # BGE-Korean / KoSimCSE 임베딩

# (옵션) STT — A6
# openai-whisper
